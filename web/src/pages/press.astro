---
/**
 * @file press.astro
 * @description The press kit in Polish (/press) — the only locale it has. Structure lives in the
 *  shared PressPage component; this route only selects the locale.
 *
 *  NO /en AND /fr YET, and `/press` is deliberately outside TRANSLATED_ROUTES: the page's Polish
 *  has just been recut and is on the copy desk for editing, and §2 of docs/specs/web-copy-desk-2026-09.md
 *  forbids translating prose that is still moving. When it settles, the order is route files
 *  first, ledger entry last (i18n/config's ordering contract).
 *
 *  The page is `noindex,follow` and is filtered out of the sitemap by hand in astro.config: its
 *  only traffic is a link we send, in the SECOND mail — after the yes.
 * @architecture Astro islands 2026
 * @module pages/press
 */
import PressPage from "../components/pages/PressPage.astro";
---

<PressPage lang="pl" />
