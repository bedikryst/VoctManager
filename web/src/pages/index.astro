---
/**
 * @file index.astro
 * @description Polish landing (`/`) — the canonical, un-prefixed page. All structure, styles,
 *  islands and JSON-LD live in the shared LandingPage component; this route only selects the
 *  locale. The /en and /fr siblings render the same component with a different `lang`. See
 *  i18n/config for the routing model and i18n/content/landing.ts for the copy.
 * @architecture Astro islands 2026
 * @module pages/index
 */
import LandingPage from "../components/pages/LandingPage.astro";
---

<LandingPage lang="pl" />
