---
/**
 * @file en/koncerty.astro
 * @description English concerts index (/en/koncerty). Structure lives in the shared KoncertyPage
 *  component; this route only selects the locale. The chrome is translated in
 *  i18n/content/koncerty.ts, and both the page's prose and each evening's own words fall back per
 *  field to their Polish until the copy desk's English lands in content/pages.en.yaml and
 *  content/concerts.en.yaml.
 * @architecture Astro islands 2026
 * @module pages/en/koncerty
 */
import KoncertyPage from "../../components/pages/KoncertyPage.astro";
---

<KoncertyPage lang="en" />
