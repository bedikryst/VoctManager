---
/**
 * @file en/polityka-prywatnosci.astro
 * @description English privacy policy (/en/polityka-prywatnosci). Structure lives in the shared
 *  PrivacyPage component; this route only selects the locale. The path keeps its Polish slug in
 *  every locale, like every other page on this site — the URL is the document's address, not a
 *  sentence, and translating it would break every inbound link the moment a wording changed.
 *  The Polish version is the binding one; § 12 of the document says so inside the document.
 * @architecture Astro islands 2026
 * @module pages/en/polityka-prywatnosci
 */
import PrivacyPage from "../../components/pages/PrivacyPage.astro";
---

<PrivacyPage lang="en" />
