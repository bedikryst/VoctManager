---
/**
 * @file koncerty.astro
 * @description Polish concerts index (/koncerty) — the canonical, un-prefixed page. All structure,
 *  styles and JSON-LD live in the shared KoncertyPage component; this route only selects the
 *  locale. The /en and /fr siblings render the same component with a different `lang`. See
 *  i18n/config for the routing model, src/content/pages/koncerty.yaml for the page's prose,
 *  src/content/concerts.yaml for what each evening says, and i18n/content/koncerty.ts for the
 *  chrome.
 * @architecture Astro islands 2026
 * @module pages/koncerty
 */
import KoncertyPage from "../components/pages/KoncertyPage.astro";
---

<KoncertyPage lang="pl" />
