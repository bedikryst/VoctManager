---
/**
 * @file kontakt.astro
 * @description Polish contact route (/kontakt) — the canonical, un-prefixed page. All structure,
 *  styles and JSON-LD live in the shared ContactPage component; this route only selects the locale.
 *  The /en and /fr siblings render the same component with a different `lang`. See i18n/config for
 *  the routing model, src/content/pages/kontakt.yaml for the prose and i18n/content/kontakt.ts for
 *  the chrome.
 * @architecture Astro islands 2026
 * @module pages/kontakt
 */
import ContactPage from "../components/pages/ContactPage.astro";
---

<ContactPage lang="pl" />
