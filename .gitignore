# =========================
# SYSTEM & IDE (Globalne)
# =========================
.DS_Store
.idea/
.vscode/
!.vscode/extensions.json
*.swp
*.swo
*~
*.suo
*.ntvs*
*.njsproj
*.sln

# =========================
# ZMIENNE ŚRODOWISKOWE
# =========================
.env
backend/.env
frontend/.env
# Backup secrets (healthcheck URL, rclone remote) — infra/backup.env.example is committed.
infra/backup.env
*.local

# =========================
# BACKEND (Django & Python)
# =========================
backend/venv/
backend/env/
backend/__pycache__/
backend/**/*.pyc
backend/db.sqlite3
*.sqlite3
backend/media/
!backend/media/.gitkeep
backend/static/
gunicorn.ctl

# =========================
# FRONTEND (React & Vite)
# =========================
frontend/node_modules/
frontend/dist/
frontend/dist-ssr/
frontend/*.tsbuildinfo
frontend/.mypy_cache/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
logs/
*.log

# --- Frontend Media Assets ---
frontend/public/**/*.jpg
frontend/public/**/*.jpeg
frontend/public/**/*.png
frontend/public/**/*.webp
frontend/public/**/*.mp4
frontend/public/**/*.mov
frontend/public/**/*.webm
frontend/public/**/*.mp3
frontend/public/samples/

# PWA app icons are essential, lightweight build assets (referenced by
# manifest.webmanifest + index.html) — they MUST ship in git, so re-include
# them despite the broad public/*.png media ignore above.
!frontend/public/icons/
!frontend/public/icons/*.png
# Same case: the collapsed desktop sidebar draws this mark (DesktopSidebar.tsx).
# Ignored, it existed only on the author's disk — `npm run dev` on a fresh clone
# showed a broken image, and the deployed panel only rendered a mark because
# nginx serves `*.png` marketing-first (infra/nginx/prod.conf) and the Astro site
# ships a file of the same name. The panel must not depend on that coincidence.
!frontend/public/logo_gold.png
voct_data/
backend/celerybeat-schedule

# --- Other ---
gemini/
gemini.md
.prettierignore

.continue/
.secrets/
.ai/
.claude/
CLAUDE.md
docs/welcome-invitation-and-email-gating-spec.md

docs/about/
